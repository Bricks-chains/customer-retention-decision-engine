# Stage 7 - Reusable scoring interface

The package modules provide a stable scoring path: clean an input frame, fit the preprocessing/model pipeline on training data, score a holdout or new population, and pass probabilities to the decision engine. Raw customer identifiers remain outside the model feature matrix but can be joined back for action delivery.
